{
  "name": "presypaci_hodiny",
  "version": "1.0.0",
  "description": "Interaktivní animace přesýpacích hodin (Lottie)",
  "private": true,
  "scripts": {
    "dev": "live-server . --port=8766 --open=/index.html --wait=300"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
